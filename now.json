{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@now/static-build" }],
  "routes": [
    { "src": "^/api/(.*)", "dest": "https://api.readhub.cn/$1" },
    { "src": "^/(js|css)/.*", "headers": { "cache-control": "max-age=31536000, immutable" }},
    { "src": ".*", "dest": "/" }
  ],
  "alias": "readhub.now.sh"
}
